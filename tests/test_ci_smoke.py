"""CI smoke placeholder (kind: placeholder_smoke).

Future scoring and adapter contract tests under tests/ MUST use scrubbed
fixtures only — no live provider or agent calls — so the main safety gate
stays deterministic and secret-free (FR-006 / FR-008).
"""


def test_ci_smoke_placeholder() -> None:
    """Temporary bootstrap check; replace/expand with real tests later."""
    assert True
